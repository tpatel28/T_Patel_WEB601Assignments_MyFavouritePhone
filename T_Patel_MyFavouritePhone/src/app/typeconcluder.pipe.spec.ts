import { TypeconcluderPipe } from './typeconcluder.pipe';

describe('TypeconcluderPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeconcluderPipe();
    expect(pipe).toBeTruthy();
  });
});
